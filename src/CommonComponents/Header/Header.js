import React from 'react';
import  "./header.scss";
import Navbar from '../Navbar';

const Header = () => {
    return (
        <header>
            <Navbar/>
        </header>
    );
}

export default Header;